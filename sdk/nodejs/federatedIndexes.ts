// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * ## # Resource: splunk.FederatedIndexes
 *
 * Creates and manages a local federated index that maps to a dataset on a federated provider.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as splunk from "@pulumi/splunk";
 *
 * const remoteMain = new splunk.FederatedIndexes("remote_main", {
 *     name: "remote-main",
 *     federatedProvider: remote.name,
 *     datasetType: "index",
 *     datasetName: "main",
 * });
 * ```
 *
 * ## Import
 *
 * Import using the short name or the full Splunk index name. In either case, set `name = "remote-main"` in configuration:
 *
 * ```sh
 * $ pulumi import splunk:index/federatedIndexes:FederatedIndexes remote_main federated:remote-main
 * ```
 *
 * Alternatively:
 *
 * ```sh
 * $ pulumi import splunk:index/federatedIndexes:FederatedIndexes remote_main remote-main
 * ```
 */
export class FederatedIndexes extends pulumi.CustomResource {
    /**
     * Get an existing FederatedIndexes resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: FederatedIndexesState, opts?: pulumi.CustomResourceOptions): FederatedIndexes {
        return new FederatedIndexes(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'splunk:index/federatedIndexes:FederatedIndexes';

    /**
     * Returns true if the given object is an instance of FederatedIndexes.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is FederatedIndexes {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === FederatedIndexes.__pulumiType;
    }

    /**
     * Remote index, saved search, scheduled search (for `lastjob`), or data model name, without a type prefix. For example, use `main`, not `index:main`. Changes to either dataset field update the mapping in place.
     *
     * Federated indexes require a standard-mode provider. Remote saved searches and data models must be readable by the provider's service account and shared globally or in the provider's app context. See [Splunk's remote dataset documentation](https://help.splunk.com/en/splunk-enterprise/search/search-manual/9.1/run-federated-searches-across-multiple-splunk-deployments/create-a-federated-index).
     */
    declare public readonly datasetName: pulumi.Output<string>;
    /**
     * Remote dataset type. Accepts only `index`, `savedsearch`, `lastjob`, or `datamodel`. Defaults to `index`.
     */
    declare public readonly datasetType: pulumi.Output<string | undefined>;
    /**
     * Name of the federated provider containing the remote dataset.
     */
    declare public readonly federatedProvider: pulumi.Output<string>;
    /**
     * Local federated index name without the `federated:` prefix. Use lowercase letters, numbers, underscores, or hyphens, starting with a letter or number. The name must be at most 2048 characters and must not contain `kvstore`. Changing it creates a new index.
     */
    declare public readonly name: pulumi.Output<string>;

    /**
     * Create a FederatedIndexes resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: FederatedIndexesArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: FederatedIndexesArgs | FederatedIndexesState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as FederatedIndexesState | undefined;
            resourceInputs["datasetName"] = state?.datasetName;
            resourceInputs["datasetType"] = state?.datasetType;
            resourceInputs["federatedProvider"] = state?.federatedProvider;
            resourceInputs["name"] = state?.name;
        } else {
            const args = argsOrState as FederatedIndexesArgs | undefined;
            if (args?.datasetName === undefined && !opts.urn) {
                throw new Error("Missing required property 'datasetName'");
            }
            if (args?.federatedProvider === undefined && !opts.urn) {
                throw new Error("Missing required property 'federatedProvider'");
            }
            if (args?.name === undefined && !opts.urn) {
                throw new Error("Missing required property 'name'");
            }
            resourceInputs["datasetName"] = args?.datasetName;
            resourceInputs["datasetType"] = args?.datasetType;
            resourceInputs["federatedProvider"] = args?.federatedProvider;
            resourceInputs["name"] = args?.name;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(FederatedIndexes.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering FederatedIndexes resources.
 */
export interface FederatedIndexesState {
    /**
     * Remote index, saved search, scheduled search (for `lastjob`), or data model name, without a type prefix. For example, use `main`, not `index:main`. Changes to either dataset field update the mapping in place.
     *
     * Federated indexes require a standard-mode provider. Remote saved searches and data models must be readable by the provider's service account and shared globally or in the provider's app context. See [Splunk's remote dataset documentation](https://help.splunk.com/en/splunk-enterprise/search/search-manual/9.1/run-federated-searches-across-multiple-splunk-deployments/create-a-federated-index).
     */
    datasetName?: pulumi.Input<string | undefined>;
    /**
     * Remote dataset type. Accepts only `index`, `savedsearch`, `lastjob`, or `datamodel`. Defaults to `index`.
     */
    datasetType?: pulumi.Input<string | undefined>;
    /**
     * Name of the federated provider containing the remote dataset.
     */
    federatedProvider?: pulumi.Input<string | undefined>;
    /**
     * Local federated index name without the `federated:` prefix. Use lowercase letters, numbers, underscores, or hyphens, starting with a letter or number. The name must be at most 2048 characters and must not contain `kvstore`. Changing it creates a new index.
     */
    name?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a FederatedIndexes resource.
 */
export interface FederatedIndexesArgs {
    /**
     * Remote index, saved search, scheduled search (for `lastjob`), or data model name, without a type prefix. For example, use `main`, not `index:main`. Changes to either dataset field update the mapping in place.
     *
     * Federated indexes require a standard-mode provider. Remote saved searches and data models must be readable by the provider's service account and shared globally or in the provider's app context. See [Splunk's remote dataset documentation](https://help.splunk.com/en/splunk-enterprise/search/search-manual/9.1/run-federated-searches-across-multiple-splunk-deployments/create-a-federated-index).
     */
    datasetName: pulumi.Input<string>;
    /**
     * Remote dataset type. Accepts only `index`, `savedsearch`, `lastjob`, or `datamodel`. Defaults to `index`.
     */
    datasetType?: pulumi.Input<string | undefined>;
    /**
     * Name of the federated provider containing the remote dataset.
     */
    federatedProvider: pulumi.Input<string>;
    /**
     * Local federated index name without the `federated:` prefix. Use lowercase letters, numbers, underscores, or hyphens, starting with a letter or number. The name must be at most 2048 characters and must not contain `kvstore`. Changing it creates a new index.
     */
    name: pulumi.Input<string>;
}
